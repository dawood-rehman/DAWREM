import AdminSectionShell from "@/components/admin/AdminSectionShell";

export default function AdminPagesPage() {
  return (
    <AdminSectionShell
      title="Page Builder"
      description="Create and maintain dynamic store pages, SEO metadata, and navigation visibility."
      points={["Dynamic Pages", "SEO", "Publishing"]}
    />
  );
}
